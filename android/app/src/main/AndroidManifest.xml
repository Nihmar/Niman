<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The library root is a user-chosen folder on shared storage, read
         and written with plain file I/O (walk, watch, open). Without "All
         files access" the FUSE layer backing /storage/emulated/0 hands the
         app a directories-only view and denies open() on notes it did not
         create. A Storage Access Framework tree grant does not help: it
         only opens the DocumentsProvider (content:// URIs), not the
         filesystem. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- Task reminders (T-TD-07): notifications are runtime-granted on
         Android 13+ (asked when the first reminder is scheduled).
         Minute precision needs exact alarms fired via
         setExactAndAllowWhileIdle, which survives Doze (screen off) and
         a closed app. Niman is a todo/reminder app (targetSdk >= 33),
         so it requests USE_EXACT_ALARM: a normal permission auto-granted
         at install with no runtime prompt, unlike SCHEDULE_EXACT_ALARM
         which is denied by default on Android 14+ and needs a manual
         system-settings toggle, silently leaving reminders unreliable
         with the screen off. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- Let the plugin's alarm receivers survive a device reboot and an
         app re-install so scheduled task reminders are re-registered.
         flutter_local_notifications (>=16) declares only the bare minimum
         in its own manifest, so the app must add these itself. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Voice notes (issue #56): the microphone is runtime-granted on
         Android 6+ (asked when the first recording starts). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:label="Niman"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Scheduled task reminders (T-TD-07): flutter_local_notifications
             needs these receivers actually declared in the app manifest to
             deliver zoned notifications after the app leaves the foreground
             (without them the alarms are scheduled but never surface). The
             boot receiver re-registers pending reminders after a reboot or
             a package re-install. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
